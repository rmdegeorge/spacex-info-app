import React from 'react';
import styled from 'styled-components';
import {withData} from '../context-providers/DataProvider';

function RocketDetails(props) {
  return (
    <div>
      
    </div>
  );
}

export default withData(RocketDetails);